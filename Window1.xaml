<Window x:Class="WpfApp1.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="Window1" Height="225" Width="400">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Rectangle Height="NaN" Grid.RowSpan="8" Grid.ColumnSpan="6" Width="NaN" Fill="{DynamicResource {x:Static SystemColors.InfoBrushKey}}"/>
        <TextBox x:Name="textBox" Grid.ColumnSpan="2" Margin="10,10,10,10" Grid.Row="1"/>
        <Label Content="Введите название нового типа записи" Grid.ColumnSpan="2" VerticalAlignment="Center" Margin="10,0,10,0" HorizontalAlignment="Center"/>
        <Button Content="Добавить новый тип записи" Margin="10,10,10,10" Grid.Row="3" Grid.ColumnSpan="2" Click="Button_Click_3"/>
    </Grid>
</Window>
